import { Logger } from './logger';
export declare class App {
    devPush: boolean;
    gcmKey: string;
    logger: Logger;
    constructor(appId: any, apiKey: any);
    id: any;
    apiKey: any;
    toString(): string;
}
